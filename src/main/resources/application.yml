spring:
  application:
    name: NailChain
  datasource:
    url: jdbc:mysql://localhost:3306/nail-system-database
    username: nholq
    password: abc123!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      jdbc:
        time_zone: UTC

server:
  port: 9090